# Bus Trip Pipeline

Data engineering pipeline for bus trip data.

## Structure
- ingestion: Data fetching scripts
- models: SQL models (raw and curated)
- config: Configuration files
- scripts: Utility scripts

## Setup
See requirements.txt for dependencies.

docker-compose up